<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:showDividers="middle"
        android:dividerPadding="@dimen/activity_vertical_margin"
        android:divider="@drawable/divider"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingTop="@dimen/activity_vertical_margin" >

        <TextView
            style="@style/MenuText"
            android:onClick="onViewAnimationClick"
            android:text="@string/view_animation" />

        <TextView
            style="@style/MenuText"
            android:onClick="onPropertyAnimationClick"
            android:text="@string/property_animation" />

        <TextView
            style="@style/MenuText"
            android:onClick="onTransitionsFrameworkClick"
            android:text="@string/transitions_framework" />

        <TextView
            style="@style/MenuText"
            android:onClick="onSharedElementsClick"
            android:text="@string/shared_elements_transitions" />

        <TextView
            style="@style/MenuText"
            android:onClick="onDrawableAnimationClick"
            android:text="@string/drawable_animation" />

    </LinearLayout>

</ScrollView>